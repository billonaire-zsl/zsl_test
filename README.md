# 待办事项提醒工具

一个基于 React 和 Vite 的网页版待办提醒工具，支持新增待办、设置提醒时间、优先级筛选、完成状态管理、本地持久化和浏览器通知。

## 功能

- 新增待办标题、备注、优先级和提醒时间
- 按全部、待处理、已到期、已完成筛选
- 自动统计全部、待处理、已到期和已完成数量
- 使用 `localStorage` 保存待办数据
- 支持浏览器通知权限，到点后弹出提醒

## 本地运行

```bash
npm install
npm run dev
```

## 构建

```bash
npm run build
```
